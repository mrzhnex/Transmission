<Window x:Class="Client.ConnectWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Transmission" SizeToContent="WidthAndHeight" ResizeMode="NoResize" MinHeight="100" MinWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="Server" Grid.Column="0" Grid.Row="0" Text="Server"/>
        <TextBox x:Name="IpAddressField" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="0" Text="127.0.0.1:5555"/>

        <TextBlock x:Name="Password" Grid.Column="0" Grid.Row="1" Text="Password"/>
        <TextBox x:Name="PasswordField" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"/>

        <Button x:Name="Connect" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" Click="Connect_Click" Content="Connect"/>
    </Grid>
</Window>
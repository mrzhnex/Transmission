<UserControl x:Class="Server.ClientControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             DataContext="{Binding Path=DataContext, RelativeSource={RelativeSource AncestorType={x:Type ContentControl}}}"
             MouseLeftButtonDown="UserControl_MouseLeftButtonDown"
             MouseDoubleClick="UserControl_MouseDoubleClick"
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid Margin="5">
        <Grid.Resources >
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="2,2,2,2" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Path=Id}"/>
            <TextBlock Grid.Column="1" Text="{Binding Path=Username}"/>
            <TextBlock Grid.Column="2" Text="{Binding Path=ClientStatus}"/>
            <TextBlock Grid.Column="3" Text="{Binding Path=ConnectionTimeSpan}"/>
        </Grid>
    </Grid>
</UserControl>
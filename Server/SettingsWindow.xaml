<Window x:Class="Server.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Transmission" SizeToContent="WidthAndHeight" ResizeMode="NoResize" MinHeight="100" MinWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ComboBox x:Name="Languages" Grid.ColumnSpan="2" SelectionChanged="Languages_SelectionChanged"/>
        <TextBlock x:Name="ServerPort" Grid.Column="0" Grid.Row="1" Text="Server port"/>
        <TextBox x:Name="ServerPortField" Grid.Column="1" Grid.Row="1" TextChanged="ServerPortField_TextChanged"/>
    </Grid>
</Window>